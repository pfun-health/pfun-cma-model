name: pfun-cma-model CI

on:
  push:
    branches: ["main"]

jobs:
  ProjectTest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install uv
        uses: astral-sh/setup-uv@v7