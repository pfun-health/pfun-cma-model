[tool.poetry]
name = "pfun-cma-model"
version = "0.1.0"
description = "PFun CMA Model App"
authors = ["Robbie Capps <robbie@pfun.me>"]
readme = "README.md"
packages = [{include = "runtime"},{include = "frontend"},{include = "embed"}]

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
